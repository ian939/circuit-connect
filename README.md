# 전류 잇기 (Circuit Connect)

전기차 충전 앱용 **리텐션 미니게임** 프로토타입입니다.
충전 대기 시간에 한 판씩 즐기고, 완주할 때마다 충전 포인트를 적립하는 구조로,
의존성 없는 **단일 HTML 파일**로 동작합니다.

## 플레이

👉 **https://ian939.github.io/circuit-connect/**

또는 `index.html`을 브라우저에서 그냥 열면 됩니다.

## 규칙

- 같은 색 단자 두 개를 드래그해서 연결합니다.
- 선끼리 교차할 수 없습니다.
- **모든 색을 연결하고 빈칸을 하나도 남기지 않아야** 클리어됩니다.

## 적립 구조

**1판 = 10원** (충전 포인트)

| 구간 | 판수 | 적립 | 비고 |
| --- | --- | --- | --- |
| 온보딩 | 5판 | **50원** | 신규 1회한. 각 단계 최초 클리어에만 지급 |
| 데일리 챌린지 | 3판/일 | **30원/일** | 완주 시 재참여 불가 |

- 사용자당 예상 지급액: **가입 시 50원 + 이후 최대 30원/일** (월 약 900원)
- 온보딩은 1회성입니다. 완료 후에는 다시 노출되지 않습니다

## 모드

### 1. 온보딩 (1~5단계)

첫 진입 시 **가이드가 자동으로 뜹니다** (드래그 시연 + 3단계 규칙). 이후엔 헤더의 `?` 버튼으로 언제든 다시 볼 수 있습니다.

5×5 / 4색에서 시작해 7×7 / 6색까지 완만하게 올라갑니다.
**5단계를 통과하면 온보딩은 완전히 사라집니다.** 이후 접속하면 탭 없이 곧바로 랭킹전 홈이 열립니다.

| 단계 | 1 | 2 | 3 | 4 | 5 |
| --- | --- | --- | --- | --- | --- |
| 보드 | 5×5·4색 | 5×5·5색 | 6×6·5색 | 6×6·6색 | 7×7·6색 |

### 2. 데일리 챌린지 (매일 3판)

| # | 이름 | 보드 |
| --- | --- | --- |
| 1 | 워밍업 | 8×8 · 7색 |
| 2 | 러시아워 | 9×9 · 8색 |
| 3 | 과부하 | 10×10 · 8색 |

- **판이 열리는 순간부터 시간이 카운트**되고, 완주 시간이 그날의 기록이 됩니다.
- 랭킹은 **최단 시간 순**.
- **완주하면 곧바로 랭킹 화면으로 이동**합니다. 내 시간·순위·적립액을 상단 배너에 보여주고,
  `챌린지 홈으로`를 누르면 3판 목록으로 돌아갑니다. 보드 화면에 머무르지 않습니다.
- 완주한 판은 **종료**됩니다. 카드를 다시 누르면 게임이 아니라 랭킹이 열립니다.
- 매일 자정에 새 문제 3개로 초기화됩니다.

## 어뷰징 차단 설계

현금성 포인트가 걸려 있어 다음 경로를 막았습니다.

| 경로 | 차단 방식 |
| --- | --- |
| 온보딩 '처음부터'로 무한 파밍 | `paidStage`에 지급 완료 단계를 기록, 재클리어 시 무지급 |
| 챌린지 '다시 시작'으로 타이머 리셋 | 챌린지 모드에서는 **'새 판' 버튼 자체를 제거** |
| 나갔다 재입장해 타이머 리셋 | 최초 입장 시각(`start`)을 저장, 재입장 시 **이어서 카운트** |
| 완주 후 재도전으로 기록 세탁 | 완주 시 판을 잠그고 카드 탭 → 랭킹으로 연결 |
| 판을 여러 번 열어 문제 고르기 | 문제는 날짜 시드로 고정 — 몇 번을 열어도 같은 문제 |

> ⚠️ **위 방어는 UX 레벨입니다.** `localStorage`는 사용자가 조작할 수 있으므로
> 실제 지급은 반드시 서버에서 재검증해야 합니다 (하루 3판 상한, 판별 1회 지급, 완주 여부).

중도 이탈해도 그려둔 선은 저장되어 이어서 풀 수 있습니다 (시간은 계속 흐름).

## 테스트 초기화

주소 뒤에 `?reset` 을 붙여 열면 저장된 진행도(온보딩·적립·오늘 기록)가 전부 지워지고
첫 진입 상태로 돌아갑니다.

```
https://ian939.github.io/circuit-connect/?reset
```


## 랭킹

로그인 기능을 붙이기 전까지는 아래 임시 ID로 표시됩니다.
본인은 `Ian`(YOU 뱃지)이고, 나머지 9명은 더미 기록입니다.

```
Ian · Cristie · Tiffny · John · Morgan · Tommy · Anders · Hazle · Judge · Andy
```

## 특징

- 레벨 생성기가 해밀턴 경로(zig-zag + backbite 셔플)를 잘라 색을 배정하므로 **항상 정답이 존재하는 판**만 나옵니다.
- 챌린지는 `날짜 + 챌린지 번호`를 시드로 쓰는 결정적 난수(mulberry32)로 생성됩니다.
  → **같은 날이면 누구에게나 똑같은 문제**가 나오므로 기록 비교가 공정합니다.
- 진행도·기록은 `localStorage`에 저장됩니다 (`circuit-connect.v2`).
- 마우스·터치 모두 지원. 태블릿 롱프레스 시 뜨는 복사/잘라내기 콜아웃 차단.
- `prefers-reduced-motion` 대응.

## 서버 연동 시 할 일

지금은 프론트만 있는 프로토타입입니다.

| 위치 | 할 일 |
| --- | --- |
| `awardPoints(won, src)` | 충전 포인트 적립 API 호출. **서버에서 상한·중복 재검증 필수** |
| `NAMES` / `ME` | 로그인 사용자 정보로 교체 |
| `botTimes()` | 실제 랭킹 조회 API로 교체 |
| `winChallenge()` | 기록 제출 API 추가 (서버 시각 기준 검증 권장) |
| `todayKey()` | 클라 로컬 날짜 → 서버 기준 날짜로 교체 (기기 시각 조작 방지) |

## 튜닝 포인트

| 상수 | 위치 | 현재 값 |
| --- | --- | --- |
| `PAY` | 판당 적립액 | `10` (원) |
| `ONBOARD_STEPS` | 온보딩 단계별 보드 (개수 = 온보딩 판수) | 5판 |
| `CHALLENGES` | 일일 챌린지 정의 (개수·크기·색) | 3판 |
| `CHALLENGES[].base` | 봇 기준 기록 (랭킹 난이도) | 170s / 255s / 360s |
